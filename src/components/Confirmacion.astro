---
const { data } = Astro.props;
---

<div class="confirmacion">
  <h1>CONFIRMA TU ASISTENCIA</h1>
  <p class="subtitulo">Tu presencia es el mejor regalo que podemos recibir. Por favor, confirma tu asistencia.</p>

  <form id="rsvp-form" class="rsvp-form">
    <div class="form-group">
      <label for="nombre">Nombre completo</label>
      <input type="text" id="nombre" name="nombre" placeholder="Escribe tu nombre" required />
    </div>

    <div class="form-group">
      <label for="acompanantes">Numero de acompanantes</label>
      <select id="acompanantes" name="acompanantes">
        <option value="0">Solo yo</option>
        <option value="1">1 acompanante</option>
        <option value="2">2 acompanantes</option>
        <option value="3">3 acompanantes</option>
      </select>
    </div>

    <div class="form-group">
      <label for="asistencia">Confirmo mi asistencia</label>
      <div class="radio-group">
        <label class="radio-label">
          <input type="radio" name="asistencia" value="si" required />
          <span>Con mucho gusto asistire</span>
        </label>
        <label class="radio-label">
          <input type="radio" name="asistencia" value="no" />
          <span>No podre asistir</span>
        </label>
      </div>
    </div>

    <div class="form-group">
      <label for="mensaje">Mensaje para los novios (opcional)</label>
      <textarea id="mensaje" name="mensaje" rows="3" placeholder="Escribe un mensaje especial..."></textarea>
    </div>

    <button type="submit" class="btn-confirmar">ENVIAR CONFIRMACION</button>
  </form>

  <div id="rsvp-success" class="success-message" style="display: none;">
    <div class="success-icon">&#10003;</div>
    <h2>Gracias por confirmar</h2>
    <p>Hemos recibido tu confirmacion. Nos vemos pronto.</p>
  </div>
</div>

<script>
  const form = document.getElementById('rsvp-form');
  const successMessage = document.getElementById('rsvp-success');

  if (form) {
    form.addEventListener('submit', (e) => {
      e.preventDefault();

      const formData = new FormData(form as HTMLFormElement);
      const data = {
        nombre: formData.get('nombre'),
        acompanantes: formData.get('acompanantes'),
        asistencia: formData.get('asistencia'),
        mensaje: formData.get('mensaje'),
      };

      console.log('RSVP:', data);

      form.style.display = 'none';
      if (successMessage) {
        successMessage.style.display = 'flex';
      }
    });
  }
</script>

<style>
  .confirmacion {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 60px 20px 80px;
    background-color: #f2f1ed;
  }

  .confirmacion > h1 {
    font-family: "Aboreto", sans-serif;
    color: #96a0a9;
    font-size: 3rem;
    font-weight: 400;
    text-align: center;
    margin-bottom: 0.5rem;
  }

  .subtitulo {
    font-family: "Andika", sans-serif;
    color: #a3a3a3;
    text-align: center;
    font-size: 1rem;
    max-width: 500px;
    margin-bottom: 2.5rem;
  }

  .rsvp-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    width: 100%;
    max-width: 450px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .form-group label:first-child {
    font-family: "Andika", sans-serif;
    color: #96a0a9;
    font-size: 0.85rem;
    letter-spacing: 0.05rem;
    text-transform: uppercase;
  }

  .form-group input[type="text"],
  .form-group select,
  .form-group textarea {
    font-family: "Andika", sans-serif;
    font-size: 1rem;
    padding: 0.75rem 1rem;
    border: 1px solid #ddd;
    border-radius: 8px;
    background-color: #fff;
    color: #555;
    outline: none;
    transition: border-color 0.2s;
  }

  .form-group input[type="text"]:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    border-color: #cea7a1;
  }

  .form-group textarea {
    resize: vertical;
  }

  .radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    margin-top: 0.3rem;
  }

  .radio-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
  }

  .radio-label input[type="radio"] {
    accent-color: #cea7a1;
    width: 18px;
    height: 18px;
  }

  .radio-label span {
    font-family: "Andika", sans-serif;
    color: #555;
    font-size: 0.95rem;
  }

  .btn-confirmar {
    font-family: "Aboreto", sans-serif;
    background-color: #cfc4c2;
    color: white;
    padding: 0.8rem 2rem;
    border: none;
    border-radius: 20px;
    cursor: pointer;
    font-size: 1rem;
    letter-spacing: 0.1rem;
    margin-top: 0.5rem;
    transition: background-color 0.2s;
    align-self: center;
  }

  .btn-confirmar:hover {
    background-color: #cea7a1;
  }

  .success-message {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    text-align: center;
    padding: 2rem;
  }

  .success-icon {
    font-size: 3rem;
    color: #cea7a1;
    width: 70px;
    height: 70px;
    border: 2px solid #cea7a1;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 0.5rem;
  }

  .success-message h2 {
    font-family: "Aboreto", sans-serif;
    color: #96a0a9;
    font-weight: 400;
    font-size: 1.8rem;
  }

  .success-message p {
    font-family: "Andika", sans-serif;
    color: #a3a3a3;
  }

  @media (min-width: 768px) {
    .rsvp-form {
      max-width: 500px;
    }
  }
</style>
